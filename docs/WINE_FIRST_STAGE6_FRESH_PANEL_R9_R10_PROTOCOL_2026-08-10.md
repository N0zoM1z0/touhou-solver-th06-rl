# Stage 6 fresh Wine panel r9-r10 (predeclared 2026-08-10)

## Purpose

Collect two new original-retail Wine first-failure episodes after the broad
sub10 source support gate failed.  The panel asks whether current frozen-UCB
physical failures expose a new repeated generic region; it does not rescue a
closed action pair or fit from adjacent frames.

The current complete-Stage point baseline is 10 physical HITs.  First-failure
survival is only a cheap data-acquisition/filtering domain.  Any eventual
candidate still has to improve fresh alternating complete natural Wine Stage
6 HIT counts.

## Frozen collection

Collect exactly two episodes named r9 and r10 before inspecting either result:

- Lunatic / Reimu-A / natural Practice Stage 6;
- original retail SHA-256
  `9f76483c46256804792399296619c1274363c31cd8f1775fafb55106fb852245`;
- native DLL SHA-256
  `71b27bef942928dfc04b6a489ef261cb6485cd5bab2cd4d4ebf4081991a22b5b`;
- policy plug-in SHA-256
  `4d7f10925731d7f83389aaa8c2aa942d7ed156de54791767ff2ced802483bbf2`;
- immutable policy-state SHA-256
  `e2c28f8e9c0bb1cf917c8204809f8cb163fe359bf7b71df4dc1f90619e3bf6a0`;
- exploration zero, frame-v5 lossless corpus, no life patch or HIT
  continuation;
- stop on first physical HIT, native-authority failure, or Bomb request;
- ordinary pipes, never a PTY, exact input release/PID/prefix cleanup.

There is no fit, state change, threshold change, anchor selection, or policy
change between r9 and r10.  Do not replace a short, unusual, singleton, or
failed episode merely to obtain a preferred context.

## Fixed post-collection audit

After both reports and corpora are transaction-complete:

1. verify executable/native/plugin/policy hashes, wrapper return, immutable
   state, zero Bomb, and empty dedicated-prefix leftovers;
2. replay all r1-r10 current-kernel prefixes through frozen UCB and require
   zero recorded-incumbent mismatch;
3. count one vote per physical episode per generic region;
4. require a region to have at least two independent episodes and at least one
   r9/r10 episode before it can be considered new evidence;
5. keep every previously closed absolute pair, exact frontier lookup, broad
   sub10 override, and sub10 source-support fit closed;
6. if no untested repeated generic opportunity remains, create zero candidates
   and return to the incumbent;
7. if one or more remain, predeclare a separate deterministic anchor/action
   protocol before any COW outcome is opened.

Existing shadow-model scores in factual replay are diagnostics only and may
not choose a candidate.  Full-stage HIT continuation, including the new
10-HIT calibration trace, remains evaluation-only and cannot enter this
corpus or any fit.
