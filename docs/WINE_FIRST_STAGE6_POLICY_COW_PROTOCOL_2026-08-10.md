# Stage 6 policy-faithful COW protocol (predeclared 2026-08-10)

## Purpose and authority

Test whether one native-safe first-action substitution can improve the actual
immutable frozen-UCB continuation that led to the repeated late Stage 6
authority failure. Unlike the closed offline-teacher scan, every later action
is selected by restored `phase-local-hierarchical-ucb-v4` with its exact
automatic policy keys.

This is still headless hypothesis generation. It cannot activate a residual or
promote a policy. Any survivor requires a new disjoint original-retail Wine
shadow episode, an active first-failure canary, and finally alternating complete
natural Wine Stage 6 HIT-count A/B. Repeatable 0-HIT full clears remain the
target. Full-Stage HIT continuation is evaluation-only and never training.

## Frozen runtime contract

- portable source commit:
  `604235a13140999e7f7239aafe8c7fd0a22ff51d`;
- Linux source binary SHA-256:
  `9369474727800457299a1fae1ca963dc073d474cac21e89bd4d62c55f21d6ae2`;
- immutable UCB state SHA-256:
  `e2c28f8e9c0bb1cf917c8204809f8cb163fe359bf7b71df4dc1f90619e3bf6a0`;
- original-retail executable SHA-256:
  `9f76483c46256804792399296619c1274363c31cd8f1775fafb55106fb852245`;
- original-retail native kernel SHA-256:
  `71b27bef942928dfc04b6a489ef261cb6485cd5bab2cd4d4ebf4081991a22b5b`;
- scope: Lunatic / Reimu-A / Stage 6 Practice;
- branch horizon: 600 ticks; policy/lookahead horizon: 12;
- Hard and advisory lookahead delivery envelope: `(0,1,2,3)`;
- physical source STEP remains synchronous;
- immutable mode suppresses every observe/update callback exactly as Wine did;
- Bomb remains forbidden; empty Hard, HIT, or incoherent context stops a branch.

The factual first-action branch must reproduce every remaining Wine action and
the exact terminal failure frame before any counterfactual outcome is accepted.

## Discovery

Use the same fixed panel-2 r5 anchor as both preceding scans:
run `20260810T134540Z-909617800`, sequence 6175, retail frame 6834. Its frozen
incumbent is `down_right`. Branch these 14 retail-native-safe and locally
admissible first actions once, in order:

`stay`, `up`, `down`, `left`, `right`, `up_left`, `up_right`, `down_left`,
`down_right`, `stay_fast`, `down_fast`, `left_fast`, `up_right_fast`,
`down_left_fast`.

All actions are eligible in this new estimand. The closed teacher-continuation
result remains unchanged, but it did not measure residual-plus-frozen-UCB
continuation and therefore cannot exclude an action here.

Rank only by the already-fixed robust tuple: clean completion, no physical
death, survival ticks, power-of-two bucketed minimum future native-safe width,
then terminal reserve buckets at `0, 8, 16, 32, 64`. Exact pixel values inside
a bucket cannot break ties.

Discovery passes only for one unique robust non-incumbent winner. Incumbent win
or any tie ends with zero candidates; do not inspect the runner-up or change
the rank.

## Conditional confirmation

Only if discovery passes, compare exactly `down_right` and that one action at
panel-2 r6 run `20260810T134906Z-860850500`, sequence 6354, retail frame 6999.
The factual `down_right` branch must reproduce r6 through its frame-7045
Hard-empty stop. The alternative must be locally admissible at the checkpoint
and strictly beat `down_right` under the same robust tuple. Otherwise it is
rejected.

One strict r5/r6 winner is a single headless hypothesis only. No nearby frames,
second-best action, additional Panel-2 anchor, horizon change, bucket change,
model fit, or threshold search is allowed after discovery.
